
{
  "entities": {
    "UserProfile": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "UserProfile",
      "type": "object",
      "description": "A user's profile containing reward system data for the ShopSphere application, enabling daily task tracking and Elite Mode gift card bonuses.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the UserProfile entity."
        },
        "username": {
          "type": "string",
          "description": "The user's display name."
        },
        "email": {
          "type": "string",
          "description": "The user's email address."
        },
        "balance": {
          "type": "number",
          "description": "The user's current accumulated monetary balance from completing daily tasks."
        },
        "streakCount": {
          "type": "number",
          "description": "The number of consecutive days the user has completed daily tasks. Resets to 0 if a day is missed."
        },
        "lastCompletedDate": {
          "type": "string",
          "description": "The UTC date and time when the user last completed a daily task. Used to track streaks and enforce the one-task-per-day rule.",
          "format": "date-time"
        },
        "eliteUnlocked": {
          "type": "boolean",
          "description": "Indicates whether the user has permanently unlocked Elite Mode by completing 365 consecutive daily tasks."
        },
        "eliteStartDate": {
          "type": "string",
          "description": "The UTC date and time when the user first achieved Elite Mode status.",
          "format": "date-time"
        },
        "eliteMonthlyCounter": {
          "type": "number",
          "description": "A counter tracking the number of daily tasks an Elite user has completed within their current 30-day cycle for earning a gift card. Resets to 0 after 30 days."
        },
        "eliteRewardsAvailable": {
          "type": "number",
          "description": "The number of $25 gift cards an Elite user has earned but not yet redeemed."
        },
        "redeemedRewardIds": {
          "type": "array",
          "description": "An array of unique identifiers referencing the RewardRedemption entities, logging the history of gift cards redeemed by this user.",
          "items": {
            "type": "string"
          }
        },
        "orderIds": {
          "type": "array",
          "items": { "type": "string" }
        },
        "withdrawalIds": {
          "type": "array",
          "items": { "type": "string" }
        }
      },
      "required": [
        "id",
        "username",
        "email",
        "balance",
        "streakCount",
        "eliteUnlocked",
        "eliteMonthlyCounter",
        "eliteRewardsAvailable"
      ]
    },
    "GiftCardCode": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "GiftCardCode",
      "type": "object",
      "description": "Represents a unique $25 gift card code managed by the system.",
      "properties": {
        "id": {
          "type": "string"
        },
        "value": {
          "type": "number"
        },
        "status": {
          "type": "string"
        },
        "assignedToRedemptionId": {
          "type": "string"
        }
      },
      "required": [
        "id",
        "value",
        "status"
      ]
    },
    "RewardRedemption": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "RewardRedemption",
      "type": "object",
      "description": "Logs an instance of a user redeeming an earned gift card.",
      "properties": {
        "id": {
          "type": "string"
        },
        "userId": {
          "type": "string"
        },
        "giftCardCodeId": {
          "type": "string"
        },
        "redemptionDate": {
          "type": "string",
          "format": "date-time"
        },
        "rewardType": {
          "type": "string"
        },
        "value": {
          "type": "number"
        }
      },
      "required": [
        "id",
        "userId",
        "giftCardCodeId",
        "redemptionDate",
        "rewardType",
        "value"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "UserProfile",
          "schema": {
            "$ref": "#/backend/entities/UserProfile"
          },
          "description": "Stores a user's profile information, daily task progress, Elite Mode status, and earned rewards. Path-based authorization.",
          "params": [
            {
              "name": "userId",
              "description": "The unique ID of the authenticated user."
            }
          ]
        }
      },
      {
        "path": "/users/{userId}/rewardRedemptions/{redemptionId}",
        "definition": {
          "entityName": "RewardRedemption",
          "schema": {
            "$ref": "#/backend/entities/RewardRedemption"
          },
          "description": "A subcollection under each user's profile logging gift card redemptions.",
          "params": [
            {
              "name": "userId",
              "description": "The unique ID of the user."
            },
            {
              "name": "redemptionId",
              "description": "The unique ID for the redemption event."
            }
          ]
        }
      },
      {
        "path": "/giftCardCodes/{giftCardCodeId}",
        "definition": {
          "entityName": "GiftCardCode",
          "schema": {
            "$ref": "#/backend/entities/GiftCardCode"
          },
          "description": "A top-level collection containing all gift card codes.",
          "params": [
            {
              "name": "giftCardCodeId",
              "description": "The unique code ID."
            }
          ]
        }
      }
    ],
    "reasoning": "Standardized to a 2-segment path for UserProfile (/users/{userId}) to ensure valid Firestore DocumentReferences. Maintained subcollection structure for redemptions."
  }
}
